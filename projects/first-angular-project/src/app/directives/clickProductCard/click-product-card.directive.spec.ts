import { ClickProductCardDirective } from './click-product-card.directive';

describe('ClickProductCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickProductCardDirective();
    expect(directive).toBeTruthy();
  });
});
