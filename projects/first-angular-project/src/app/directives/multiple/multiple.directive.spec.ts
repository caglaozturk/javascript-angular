import { MultipleDirective } from './multiple.directive';

describe('MultipleDirective', () => {
  it('should create an instance', () => {
    const directive = new MultipleDirective();
    expect(directive).toBeTruthy();
  });
});
