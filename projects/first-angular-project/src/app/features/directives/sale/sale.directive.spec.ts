import { SaleDirective } from './sale.directive';

describe('SaleDirective', () => {
  it('should create an instance', () => {
    const directive = new SaleDirective();
    expect(directive).toBeTruthy();
  });
});
